# Google Cloud Platform configuration
GCP_PROJECT_ID=your-project-id       # Replace with your actual GCP project ID
GCP_REGION=us-central1               # GCP region for deployments

# Service configuration
PORT=8080                            # Local port to expose the API
SERVICE_NAME=byo-unstructured-api    # Name for the service/container

# API Authentication
BYO_UNSTRUCTURED_API_KEY=default-dev-key  # API key for development